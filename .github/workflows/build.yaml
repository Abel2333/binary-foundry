name: Build Assets

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      RELEASE_TAG: dev-${{ github.sha }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install Nushell
        uses: hustcer/setup-nu@v3

      - name: Verify Podman
        run: podman --version

      - name: Build all assets
        run: nu scripts/run-build.nu --all --release-tag ${{ env.RELEASE_TAG }}

      - name: Generate manifest
        run: nu scripts/generate-manifest.nu --release-tag ${{ env.RELEASE_TAG }}

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: binary-foundry-dist
          path: dist/*
          if-no-files-found: error
